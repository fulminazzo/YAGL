[versions]
delombok = "8.4"
groovy = "4.0.16"

fulmicollection = "1.5.2"
yamlparser = "1.6.2"

spigot = "1.14-R0.1-SNAPSHOT"
spigot_obsolete = "1.8.8-R0.1-SNAPSHOT"
spigot_legacy = "1.12.2-R0.1-SNAPSHOT"
spigot_latest = "1.20-R0.1-SNAPSHOT"

lombok = "1.18.30"
annotations = "24.1.0"

junit = "5.9.1"
mockito = "4.11.0"
jbukkit = "2.1"

[libraries]
groovy = { module = "org.apache.groovy:groovy", version.ref = "groovy" }
fulmicollection = { module = "it.fulminazzo:FulmiCollection", version.ref = "fulmicollection" }
yamlparser = { module = "it.fulminazzo:YAMLParser", version.ref = "yamlparser" }

spigot = { module = "org.spigotmc:spigot-api", version.ref = "spigot" }
spigot_obsolete = { module = "org.spigotmc:spigot-api", version.ref = "spigot_obsolete" }
spigot_legacy = { module = "org.spigotmc:spigot-api", version.ref = "spigot_legacy" }
spigot_latest = { module = "org.spigotmc:spigot-api", version.ref = "spigot_latest" }

lombok = { module = "org.projectlombok:lombok", version.ref = "lombok" }
annotations = { module = "org.jetbrains:annotations", version.ref = "annotations" }

junit-platform = { module = "org.junit:junit-bom", version.ref = "junit" }
mockito = { module = "org.mockito:mockito-inline", version.ref = "mockito" }
jbukkit = { module = "it.fulminazzo:JBukkit", version.ref = "jbukkit" }
jbukkit_legacy = { module = "it.fulminazzo.JBukkit:legacy", version.ref = "jbukkit" }